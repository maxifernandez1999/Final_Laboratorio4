import { HorarioEspecialidad } from './horario-especialidad';

describe('HorarioEspecialidad', () => {
  it('should create an instance', () => {
    expect(new HorarioEspecialidad()).toBeTruthy();
  });
});
