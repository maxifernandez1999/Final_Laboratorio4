import { BtnsBienvenidoDirective } from './btns-bienvenido.directive';

describe('BtnsBienvenidoDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnsBienvenidoDirective();
    expect(directive).toBeTruthy();
  });
});
