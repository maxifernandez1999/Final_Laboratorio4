import { BtnUsuariosDirective } from './btn-usuarios.directive';

describe('BtnUsuariosDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnUsuariosDirective();
    expect(directive).toBeTruthy();
  });
});
