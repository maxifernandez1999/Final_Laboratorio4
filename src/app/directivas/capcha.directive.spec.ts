import { CapchaDirective } from './capcha.directive';

describe('CapchaDirective', () => {
  it('should create an instance', () => {
    const directive = new CapchaDirective();
    expect(directive).toBeTruthy();
  });
});
