import { CardsLoginDirective } from './cards-login.directive';

describe('CardsLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new CardsLoginDirective();
    expect(directive).toBeTruthy();
  });
});
